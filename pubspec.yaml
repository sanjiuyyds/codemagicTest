name: ipa_min
description: Liquid glass inspector for Codemagic IPA builds.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  ios_inspect:
    path: packages/ios_inspect

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
