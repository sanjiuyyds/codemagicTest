name: ipa_min
description: XsTools by Xsca1e.
publish_to: "none"
version: 1.1.2+4

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  ios_inspect:
    path: packages/ios_inspect

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
